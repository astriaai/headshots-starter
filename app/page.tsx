import { createServerComponentClient } from "@supabase/auth-helpers-nextjs";
import { cookies } from "next/headers";
import Link from "next/link";
import { redirect } from "next/navigation";

import hero from "/public/hero.png";

import { Button } from "@/components/ui/button";
import ExplainerSection from "@/components/ExplainerSection";
import PricingSection from "@/components/PricingSection";

export const dynamic = "force-dynamic";

export default async function Index() {
  const supabase = createServerComponentClient({ cookies });

  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (user) {
    return redirect("/overview");
  }

  return (
    <div className="flex flex-col items-center pt-16">
      <div className="flex flex-col items-center w-full max-w-6xl gap-8 p-8 lg:flex-row">
        <div className="flex flex-col w-full space-y-4 lg:w-1/2">
          <h1 className="text-5xl font-bold">
            Professional AI Headshots in minutes.
          </h1>
          <p className="text-lg text-gray-600 dark:text-gray-400">
            Elevate your online presence with HD headshots generated by our AI.
            Ideal for social profiles, resumes, and professional portfolios.
          </p>
          <div className="flex flex-col space-y-2">
            <Link href="/login">
              <Button className="w-full lg:w-1/2 dark:text-gray-100">Get Your Headshots</Button>
            </Link>
            <p className="text-sm italic text-gray-700 dark:text-gray-500">
              Trusted by professionals worldwide. Quick and efficient.
            </p>
          </div>
          <div className="mt-4 text-gray-700 dark:text-gray-400">
            <span>Already a member? </span>
            <Link className="text-blue-600 hover:underline" href="/login">
              Sign In
            </Link>
          </div>
        </div>
        <div className="w-full mt-8 lg:w-1/2 lg:mt-0">
          <img
            src={hero.src}
            alt="AI Headshot Illustration"
            className="object-cover w-full h-full rounded-lg"
          />
        </div>
      </div>
      <ExplainerSection />
      <PricingSection />
    </div>
  );
}
